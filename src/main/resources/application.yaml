# Configurações do MongoDB
spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: registration

# Folder to import files
file:
  import:
    input:
      path: "C:/Files/customer-account-import/input"
    output:
      path: "C:/Files/customer-account-import/output"

# Configurações da API
api:
  health:
    url: http://localhost:8080/api/health
